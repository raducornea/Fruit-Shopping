<% if(cookie_username === undefined) { %>
    <a href="/autentificare">Autentificare</a>
<% } else { %>
    <p>Bine ai revenit, <%=cookie_username%>!</p>
<% } %>
<br>
<a href="/creare-bd">
    <input type="submit" value="Creare BD" />
</a>
<br>
<a href="/inserare-bd">
    <input type="submit" value="Încarcare BD" />
</a>

<%- html_content %>
<% if(cookie_username === undefined) { %>
    <script>
        var add_to_cart_elements = document.getElementsByClassName("ADD_TO_CART");
        for(const add_to_cart of add_to_cart_elements){
            add_to_cart.disabled = true;
        }
    </script>
<% } else { %>
    <script>
        var add_to_cart_elements = document.getElementsByClassName("ADD_TO_CART");
        for(const add_to_cart of add_to_cart_elements){
            add_to_cart.disabled = false;
        }
    </script>
<% } %>